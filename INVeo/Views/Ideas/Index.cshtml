@model IEnumerable<INVeo.Models.Idea>
@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

@{
    ViewData["Title"] = "Index";
}

@if (User.IsInRole("Admin"))
{
    
<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Brief)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsAccepted)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdeaFile)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Brief)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsAccepted)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IdeaFile)
                </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
}



            <!--test-->
            
<title>Sample Page</title>
<style>

    .main-content {
        padding: 20px;
        display: flex;
        flex-wrap: wrap;
    }

    .card {
        background-color: #f1f1f1;
        border: 1px solid #ddd;
        box-shadow: 2px 2px 5px rgba(0, 0, 0, 0.1);
        padding: 20px;
        margin: 10px;
        flex: 1 0 250px;
    }

    .user-info {
        background-color: #fff;
        border-radius: 0.5rem;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        padding: 40px;
        margin-bottom: 30px;
    }

</style>


@if (SignInManager.IsSignedIn(User))
{
    <div class="container text-center">
        <!--Information about Investor Section -->
        <section class="user-info">
            <h2>Investor Name: @User.Identity?.Name</h2>
            <a class="btn btn-secondary" asp-controller="Home" asp-action="ViewContract">View Contracts</a>
        </section>
    </div>
    
}


<!-- Idea brief section -->
<h2>Ideas</h2>
<div class="main-content">
    @foreach (var item in Model){
    <div class="card">
        @Html.DisplayFor(modelItem => item.Name)
        <p>Idea brief</p>
        @Html.DisplayFor(modelItem => item.Brief)
            <a asp-action="DetailsForInvestor" asp-route-id="@item.Id">Details</a>
    </div>
    }
</div>

<!-- Request section -->
<h2>Requests</h2>
<div class="main-content">
    <div class="card">
        <h2>Request Details</h2>
        <p>Request Details...</p>
        <p>Social Media</p>
        <p>P...</p>
    </div>
    <div class="card">
        <h2>Request Details</h2>
        <p>Request Details...</p>
        <p>Social Media</p>
        <p>P...</p>
    </div>
</div>